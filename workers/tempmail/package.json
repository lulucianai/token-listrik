{
  "name": "token-listrik-tempmail-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Email Worker + REST API compatible with token-listrik createTempmail/pollTempmail",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
